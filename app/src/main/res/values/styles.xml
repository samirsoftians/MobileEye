<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/end</item>
        <item name="colorPrimaryDark">@color/end</item>
        <item name="colorAccent">@color/end</item>
    </style>

    <style name="CustomTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/end</item>
        <item name="colorPrimaryDark">@color/end</item>
        <item name="colorAccent">@color/end</item>
        <item name="contentInsetStart">0dp</item>
        <item name="contentInsetEnd">0dp</item>
    </style>
    <style name="MyActionBar" parent="@style/Base.Widget.AppCompat.ActionBar">
        <item name="contentInsetStart">0dp</item>
        <item name="contentInsetEnd">0dp</item>
    </style>

    <style name="PreferencesTheme" parent="Theme.AppCompat.Light">
        <item name="colorPrimary">@color/Gold</item>
        <item name="android:actionBarStyle">@color/Gold</item>
        <item name="android:background">@color/DimGray</item>
        <item name="android:textColor">@color/White</item>
        <item name="colorAccent">@color/Gold</item>
        <item name="android:actionBarItemBackground">@color/Gold</item>
    </style>


    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

</resources>
